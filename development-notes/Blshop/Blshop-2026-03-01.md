## 2026-03-01

Начал подготавливать проект к деплою на **GitHub Pages**.

Поразбирался в Google, как это вообще делается. Вроде разобрался.
Решил, что пока деплоить будем ветку `develop` при каждом пуше в неё.

Создал файл `.github/workflows/deploy-develop-spa.yml`.
В нём при пуше в ветку `develop` собирается и деплоится наш проект.

Изначально использовал `peaceiris/actions-gh-pages@v6`, но почему-то поставил версию 6, хотя актуальная стабильная — версия 4.
Из-за этого перешли на пакет `gh-pages`, потратили кучу времени на борьбу с проблемами аутентификации — и в итоге всё равно вернулись к `peaceiris/actions-gh-pages@v4`, потому что она оказалась самой беспроблемной.

Провели общий созвон команды.
Обговорили текущее положение дел, рассмотрели, что нужно сделать в ближайшее время.
Готовимся к **Week 2 Checkpoint**.