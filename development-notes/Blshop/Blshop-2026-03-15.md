## 2026-03-15

## Обновление по разработке

Удалось доработать компонент подсветки кода в тексте. Сейчас код работает корректно.

Однако пока остаётся достаточно много ошибок в других элементах, связанных с типизацией.
Из-за этого приходится временно переименовывать файлы в `.py`, чтобы иметь возможность запушить ветку в Git и открыть её на рабочем компьютере. Такой небольшой лайфхак помогает обойти текущие ограничения.

На данный момент осталось доработать **2 варианта вопросов**.

Вариант с **drag-and-drop** было решено пока отложить, так как его реализация требует слишком много времени на текущем этапе разработки.