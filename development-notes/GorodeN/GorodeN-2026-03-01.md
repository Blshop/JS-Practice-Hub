## День 5 (01.03.2026)

Выполнил ревью базовых компонентов (@DmitryAstapenko, [PR](https://github.com/Blshop/JS-Practice-Hub/pull/4)). Оставил комментарии, пояснения, рекомендации.

Создал Issues в GitHub, добавил описания, assignee.

Дал рекомендации по настройке CI.

Вечером устроили созвон с командой. Обсудили ход выполнения работы каждого, а также дальнейшие планы.
Настроили CI Workflow. Сделали деплой.

Добавил ссылку на деплой в README.

Приступил к реализации авторизации и регистрации на основе базовых компонентов.

## Code Review

- [PR #4: feat: basic UI components (Button, Text, Input, Checkbox, Radio, Select)](https://github.com/Blshop/JS-Practice-Hub/pull/4) — 11 комментария (посоветовал добавить `children` типы компонента `Button`, улучшить доступность путем добавления aria-атрибутов, исполльзовать компонент `Text`, что повысит консистентность; похвалил использование `React.useId()` в компоненте `Checkbox`; предложил применение `ref` в компоненте `Input`, чтобы родительские компоненты могли получать прямой доступ к DOM-элементу; обратил внимание на отсутствие пропа `error` в компоненте `Radio`; порекомендовал добавить rest-пропсы `(...props)` в компонент `Select` на случай, если будет необходимо передать какие-то доп. атрибуты (например `id`); указал на то, что стоит переименовать константу `Component` на `Tag` в компоненте `Text` для ясности)
