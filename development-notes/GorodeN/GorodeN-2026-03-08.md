## День 9 (08.03.2026)

Приступил к реализации валидации форм авторизации и регистрации при помощи React Hook Form (производительная библиотека форм) + Zod (декларативная валидация данных).

Установил зависимости: `react-hook-form`, `@hookform/resolvers`, `zod`.

Придумал следующие правила:
- Почта (Email) должна быть валидна
- Пароль должен содержать минимум 6 символов
- Пароль должен содержать минимум 1 заглавную букву
- Пароль должен содержать минимум 1 цифру

Начал переписывать компоненты `LoginForm` и `RegisterForm`