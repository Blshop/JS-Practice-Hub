## День 10 (09.03.2026)

Реализовал валидацию форм авторизации и регистрации при помощи React Hook Form и Zod по правилам:
- Имя пользователя при регистрации является обязательным
- Почта (Email) должна быть валидна
- Пароль должен содержать минимум 6 символов
- Пароль должен содержать минимум 1 заглавную латинскую букву
- Пароль должен содержать минимум 1 цифру

Решил создать схемы валидации в отдельных файлах `loginSchema.ts` и `registerSchema.ts` с экспортом типов.
Компоненты `LoginForm` и `RegisterForm` переписаны: теперь используют `useForm` с `zodResolver`, управление состоянием полей через `register`, отображение ошибок через пропс `error` компонента `Input`. Кнопки отправки блокируются (`disabled`) при отправке формы.

Выполнил ревью реализации главной страницы (Main Page) (@DmitryAstapenko, [PR#40](https://github.com/Blshop/JS-Practice-Hub/pull/40)). Оставил комментарии и рекомендации.

Отправил ребатам на ревью [PR#42](https://github.com/Blshop/JS-Practice-Hub/pull/42) с реализацией валидации форм авторизации и регистрации.

Подготовил небольшой сценарий для видео по "Tandem: Week 3 Checkpoint", чтобы меньше переживать, не пропустить что-то важное, а также сократить потенциальное число дублей. Подготовился по сценарию.

Записал видео: https://www.youtube.com/watch?v=aOlBaKt1tEI


## Code Review

- [PR #40: feat: main page layout)](https://github.com/Blshop/JS-Practice-Hub/pull/40) — 4 комментария (указал на то, что для бургер-меню можно убрать ивент по нажатию кнопки `Escape`; напомнил про алиасы, указав на место, где стоит заменить; предложил в компоненте `Header` придумать и задать пути для `Profile` и `About`, хотя эти страницы еще и не реализованы, чтобы не оставлять кнопки в `disabled`, т.к. про это можно забыть, либо кто-то может потерять время на поиск причины, почему кнопки задизейблены; указал на возможность добавления `aria-hidden="true"`, чтобы скринридеры игнорировали декоративные span-элементы в `Header`)