## День 14 (16.03.2026)

Вынес моки из `AuthStore` в отдельный модуль `src/__mocks__/mockAuth.ts`.

Установил jest с необходимыми пакетами и настроил конфиг с поддержкой TypeScript, алиасов и CSS-модулей (при помощи ИИ и документации):
- `jest` - сам тестовый фреймворк
- `@types/jest` - типы для Jest (для TypeScript)
- `@testing-library/react` - рендеринг React-компонентов в тестах
- `@testing-library/jest-dom` - расширенные матчеры (например, `toBeInTheDocument()`)
- `jest-environment-jsdom` - эмуляция браузерного окружения
- `ts-jest` - пресет для работы с TypeScript (ускоряет трансформацию)
- `babel-jest` и пресеты - для Babel
- `identity-obj-proxy` - для мока CSS-модулей

Написал 11 тестов для схем валидации Авторизации/Регистрации — проверил корректность реакций на правильные и неправильные входные данные.

Убедился, что все тесты проходят (`npm test`).

Столкнулся с трудностями при изначальном написании тестов - не правильно настроил конфиг и нехватало установки дополнительных пакетов. Пришлось обратиться за помощью к ИИ. Обдумывал перейсти с jest на vitest, но удалось разобраться.

Создал [PR #58: Авторизация/Регистрация. Тесты)](https://github.com/Blshop/JS-Practice-Hub/pull/58).

Передал команде в чате информацию о проделанной работе с пояснениями.


## Code Review

Изучал код компонента `Button` (`src/components/Button`), который был реализован Димой (@DmitryAstapenko).
Обратил внимание на лоадер. Подумал, почему бы не вынести в отдельный `Loader`? Лоадер может понадобиться и в других местах. 
Как минимум - согласно "Tandem: Week 5 Checkpoint" должен быть Loading state при загрузке данных (API-запрос, переход между страницами) с отображением индикатора загрузки, в качестве которого можно будет использовать отдельный `Loader`.
Поставил задачу и передал Диме.