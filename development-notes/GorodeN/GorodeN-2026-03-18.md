## День 16 (18.03.2026)

Применил компонент `PrivateRoute` в конфиге роутов (`src/config/routesConfig.tsx`):

- Страницы `Main` и `Profile` обернул в `PrivateRoute` (доступны только авторизованным)
- Страницы `About` и `Demo` оставил открытыми для всех
- Маршрут авторизации (`/auth/*`) обёрнут в `PrivateRoute` с параметром `inverted` (доступен только неавторизованным)

Протестировал применение `PrivateRoute`.

Создал [PR #72: Авторизация/Регистрация. Разграничение доступов](https://github.com/Blshop/JS-Practice-Hub/pull/72) с описанием выполненной работы по разграничению доступов для авторизованных и неавторизованных пользователей. Отправил ребятам на ревью.

Сделал рефакторинг тестов `Quiz` Никиты (@Blshop) ([PR #73: Refactor/quiz tests to jest](https://github.com/Blshop/JS-Practice-Hub/pull/73)).

Также задал ребятам вопрос в чате, не против ли они добавления [react-toastify](https://www.npmjs.com/package/react-toastify) для красивых уведомлений. Буду ждать ответа, реализую и применю после согласия всех.

## Code Review

- Изучал [PR #68: feat: add tests for quiz page](https://github.com/Blshop/JS-Practice-Hub/pull/68) - обратил внимание, что Никита (@Blshop) сделал достаточно много изменений в конфиге (`vite.config.ts`), установил отличные от моих и Димы (@DmitryAstapenko) пакеты, и вовсе использовал фреймворк Vitest для тестов, а не Jest, который использовали мы. Решил, что изменения могут затронуть текущую работу и надо будет многое перетесчивать, а также, что нехорошо использовать 2 фреймворка (Jest и Vitest) для тестов в одном проекте. Написал по этому поводу Никите (@Blshop) - он попросил сделать PR с рефакторингом на Jest в его ветку. Сделал [PR #73: Refactor/quiz tests to jest](https://github.com/Blshop/JS-Practice-Hub/pull/73) - подлил develop и main, откатил изменения конфига, переделал тесты под Jest, добавил `src\hooks\__mocks__\useQuestions.tsx` (для корректной работы тестов), проверил, что тесты проходят корректно. Отправил на ревью.
