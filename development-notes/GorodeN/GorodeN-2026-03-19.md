## День 17 (19.03.2026)

Получил положительный ответ от ребят по поводу добавления в проект `react-toastify`. Поставил соответствующую задачу в Trello и Issue в GitHub, после чего приступил к реализации.

Установил пакет [react-toastify](https://www.npmjs.com/package/react-toastify), подключил `ToastContainer` в `App`, создал утилиту для уведомлений (`src/utils/notify.ts`), интегрировал с `AuthStore`, удалил лишние элементы отображения ошибок.
Создал [PR #76: Уведомления. React-Toastify](https://github.com/Blshop/JS-Practice-Hub/pull/76) и отправил его ребятам на ревью/ознакомление.

Выполнил ревью [PR #71: feat: main page content](https://github.com/Blshop/JS-Practice-Hub/pull/71) Димы (@DmitryAstapenko).

Поставил задачу в Trello и Issue в GitHub на реализацию страницы 404 в соответствии с `Tandem: Week 5 Checkpoint`. Приступлю к реализации завтра, а пока придумаю решение.


## Code Review

- [PR #71: feat: main page content](https://github.com/Blshop/JS-Practice-Hub/pull/71) — 4 комментария (задал вопрос по поводу наличия стилей для `::after` прогрессбара, при том, что для него не заданы `width`, `height`, `background` - т.е. он даже не отображается, поэтому порекомендовал добавить эти стили, если все же этот псевдоэлемент необходим; порекомендовал предусмотреть случай, когда `LearningPath` получает пустой `modules`; порекомендовал заменить функцию `getVariantByStatus` на объект-константу; оставил положительный комментарий о проделанной работе)
