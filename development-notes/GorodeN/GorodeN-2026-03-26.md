## День 24 (26.03.2026)

Продолжил написание кода в `server` (также подсматривал в видео на Youtube - [Автор: Ulbi TV | Продвинутая JWT авторизация на React и Node js. Access, refresh, активация по почте](https://www.youtube.com/watch?v=fN25fMQZ2v0) и прибегал к помощи/подсказкам нейросети в непонятных для меня местах, а также для рефакторинга).

Реализовал:
- `server/src/models/User.js` - модель пользователя
- `server/src/services/tokenService.js` - сервис для работы с токенами
- `server/src/controllers/authController.js` - контроллеры авторизации (содержит всю бизнес-логику: регистрация, логин, обновление токена, выход)
- `server/src/middleware/authMiddleware.js` - Middleware для проверки access токена
- `server/src/routes/authRoutes.js` - маршруты, связанные с авторизацией
- `server/src/index.js` - основной файл сервера (создает Express-приложение, подключает middleware, монтирует маршруты, запускает сервер)

Корректировал:
- `server/.env` - сгенерировал подходящие `JWT_ACCESS_SECRET` и `JWT_REFRESH_SECRET`, исправил `MONGO_URI`
- `server/src/config/env.js` - добавил порт по умолчанию
- `server/src/config/db.js` - обернул в try-catch и добавил логирование

Добавил:
- `server/nodemon.json` - чтобы в будущем добавить конфигурацию nodemon
- `server/src/middleware/errorMiddleware.js` - чтобы в будущем реализовать Middleware