## День 25 (27.03.2026)

Занимался рефакторингом кода в `server` (в том числе с помощью ИИ, который указал на некоторые моменты):

- `server/src/controllers/authController.js` - добавил получение профиля `getProfile()`
- вынес из `server/src/models/User.js` в `server/src/models/RefreshToken.js` коллекцию для refresh-токенов
- обратил внимание на то, что `username` был отмечен как уникальное поле - сделал неуникальным

При запуске столкнулся с ошибкой, т.к. был пустой `server/nodemon.json` - решил убрать и проверить без него, и все сработало корректно.

Запустил сервер и протестировал его работу через Postman.
