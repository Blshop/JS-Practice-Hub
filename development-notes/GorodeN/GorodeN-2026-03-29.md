## День 27 (29.03.2026)

Продолжил обновлять фронтенд (также руководствовался видео на Youtube - [Автор: Ulbi TV | Продвинутая JWT авторизация на React и Node js. Access, refresh, активация по почте](https://www.youtube.com/watch?v=fN25fMQZ2v0) и прибегал к помощи ИИ для исправления багов).

Добавил и обновил:
- в `src/services/api.js` интерсепторы запросов и ответов (столкнулся с проблемой, когда при попытке авторизации под пользователем, который ранее не был зарегистрирован, страница просто обновлялась без визуальной ошибки - в интерцепторе добавил проверку, что запрос не является логином или регистрацией)
- в рамках рефакторинга добавил `src/services/authService.ts` с методами для работы с аутентификацией
- по советам ИИ установил `concurrently` и добавил команду `"dev:all": "concurrently \"npm run dev\" \"npm run dev:server\"",` (для одновременного старта фронта и сервера)
- отдельно добавил команду `"dev:server": "cd server && npm run dev"` (для старта сервера)
- по советам ИИ установил в `server` `swagger-jsdoc` (для сбора спец. комментариев) и `swagger-ui-express` (для визуализации) для Swagger (API) документации, добавил `server/src/config/swagger.js` с конфигурацией для автоматической генерации документации по аннотациям `@swagger`, импортировал Swagger и добавил роут `/api-docs` с его применением в `server/src/index.js`
- в `server/src/controllers/authController.js` описал аннотации `@swagger`
- обновил `README.md` - добавил инструкцию по локальному запуску (с требованиями и шагами по установке/запуску), описал переменные окружения, добавил ссылку на Swagger (Документация API),

Рефакторинг и баги:
- разделил `src/services/api.js` (настройка `axios`) и `src/services/authService.ts` (методы для работы с аутентификацией)
- `src/store/AuthStore.ts` - обновил прямые вызовы на методы `authService.ts`
- обратил внимание, что плохо хранить токен в localStorage, поэтому обновил `src/store/AuthStore.ts`, `src/services/authService.ts` и `src/services/api.ts` (хранение access token в памяти (сторе) и при каждой перезагрузке страницы запрашивание нового через refresh)
- после обновления страницы авторизация стала сбрасываться - не мог найти причину (видимо уже глаз замылился), поэтому спросил у ИИ, и по его совету обновил в `server/src/controllers/authController.js` метод refresh, чтобы он возвращал также данные пользователя
- столкнулся с проблемой, что после обновления страницы на какое-то время была видна страница авторизации/регистрации, а затем происходил редирект на профиль, хотя пользователь перед обновлением страницы находился в другом разделе - обновил компонент `PrivateRoute.tsx` (добавил проверки на загрузку данных (`authStore.isLoading`) и на ошибки (`authStore.sessionError`) с отображением `LoadingOverlay` (спиннер, а при ошибке - сообщение и кнопка повтора))

Создал и отправил на проверку команде [PR #94: Авторизация/Регистрация. Backend](https://github.com/Blshop/JS-Practice-Hub/pull/94) с детальным описанием и пояснениями. Обновил лейблы соответствующих Issue GitHub и тикета в Trello.

Выполнил повторное ревью [PR #87: Страница с тестами](https://github.com/Blshop/JS-Practice-Hub/pull/87) (@Blshop) после исправлений.


## Code Review

- [PR #87: Страница с тестами](https://github.com/Blshop/JS-Practice-Hub/pull/87) - 5 комментариев (указал на то, что `QuizPage` стоит обернуть в `PrivateRoute`, чтобы только авторизованные имели доступ; повторно указал на проблему, что если `totalQuestions === 0` (например, загружен JSON-файл с пустым массивом вопросов), тогда будет `Quiz Completed!` с `0 / 0`, и оставил рекомендации по исправлению с примером кода; указал на нехватку `Header` и `Footer` (навигации), и предположил, что не все (свежие) изменения подлиты; рекомендовал поправить проверку введенного ответа в тестах, т.к., например, ответ `symbol` засчитывается как неправильный, а правильный - (кавычка)symbol(кавычка); рекомендовал убрать лишний блок после правильного ответа)