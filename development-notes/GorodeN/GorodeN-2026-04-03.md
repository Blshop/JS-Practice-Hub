## День 32 (03.04.2026)

Создал Issue GitHub и тикет Trello по рефакторингу установки кук, обновлению `.env.example` и `README.md`.

1. Вынес настройки cookie в отдельную утилиту `server/src/utils/cookie.js` с ф-цией `getCookieOptions`, которая принимает время жизни cookie в миллисекундах (по умолчанию 7 дней), и в зависимости от переменной окружения `NODE_ENV` устанавливает:

- `secure: false` для локальной разработки (иначе cookie не будет отправлена через HTTP)
- `sameSite: 'lax'` для разработки (вместо 'none', который требует HTTPS)

2. Для `.env.example:`

- Добавил пользователя MongoDB с ограниченными правами (к конкретной БД - `js-practice-hub`)
- Добавил вместо нерабочего примера `MONGO_URI` реальный URI с данными нового пользователя с ограниченными правами, чтобы проверяющим не было необходимости создавать и настраивать БД в MongoDB
- Добавил комменты к переменным

3. Добавил новые данные и детали в `README.md`

Создал [PR #111: feat(App): Backend. Куки,".env.example", обновление "README.md"](https://github.com/Blshop/JS-Practice-Hub/pull/111) и отправил его ребятам для ознакомления. Оставил пояснения о том, что необходимо, чтобы проект можно было запустить:

- локально
- на хостинге
- на gh-pages

Планирую на очередном командном созвоне вместе с Никитой (@Blshop), т.к. репа принадлежит ему: захостить сервер (сейчас захосчен мой форк проекта) так же на Render, все настроить, в т.ч. автодеплой, и прикрутить сервер к gh-pages. Тогда у нас будет:

- проект работать полностью на Render (клиент+сервер через мой форк) https://js-practice-hub-frontend.onrender.com/
- клиент на gh-pages https://blshop.github.io/JS-Practice-Hub/ с сервером на хосте Render Никиты (будем мержить в ветку `develop`, будет автодеплой клиента и сервера)
- проверяющие при необходимости смогут склонить проект и запустить все локально по инструкции
